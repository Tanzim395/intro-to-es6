//ta ta! bye bye var! Gelam! raat voira prem koiro :D
let balance = 1240;
balance = 1340;
let userName = 'django phosphoras phosphoras';
let weTogether = 'Pythom' + userName;
console.log(userName);
// userName = 'mongo db';

const numbers = [45, 24, 69, 60];
//numbers = [99, 145, 3]; //not allowed to reassign
numbers.push(555);//allow to push
numbers[1] = 333;

let sum = 0;
for (let i = 0; i < numbers.length; i++) {
    const number = numbers[i];
    console.log(number);
    sum = sum + number;
}

// const student = { roll: 101, name: 'mokles', job: 'intern' };
// student.name = 'Mock Less';